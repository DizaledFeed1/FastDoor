<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="create-door-limits-table" author="Dizaled">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="door_limits"/>
            </not>
        </preConditions>

        <createTable tableName="door_limits">
            <column name="id" type="UUID">
                <constraints primaryKey="true" primaryKeyName="pk_door_limits"/>
            </column>

            <column name="limit_date" type="DATE">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_door_limits_limit_date"/>
            </column>
            <column name="front_door_quantity" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="in_door_quantity" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="availability" type="BOOLEAN" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <sql dbms="postgresql">
            ALTER TABLE door_limits ALTER COLUMN id SET DEFAULT gen_random_uuid();
        </sql>
        <sql dbms="h2">
            ALTER TABLE door_limits ALTER COLUMN id SET DEFAULT RANDOM_UUID();
        </sql>

    </changeSet>


</databaseChangeLog>